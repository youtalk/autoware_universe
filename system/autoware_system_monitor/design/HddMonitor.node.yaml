autoware_system_design_format: 0.3.0

# node information
name: HddMonitor.node

package:
  name: autoware_system_monitor
  provider: autoware

launch:
  plugin: HddMonitor
  executable: hdd_monitor
  node_output: screen

# interfaces
# diagnostics are advertised to /diagnostics via diagnostic_updater.
subscribers: []

publishers:
  - name: diagnostics
    message_type: diagnostic_msgs/msg/DiagnosticArray
    global: /diagnostics
  - name: hdd_status
    message_type: tier4_external_api_msgs/msg/HddStatus
    remap_target: ~/hdd_status

# parameters
param_files:
  - name: param_file
    default: config/hdd_monitor.param.yaml

param_values: []

# processes
processes:
  - name: monitor
    trigger_conditions:
      - on_timer: true
    outcomes:
      - to_output: diagnostics
